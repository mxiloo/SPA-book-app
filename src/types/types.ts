import React from "react";

export type TData = {
    title?: string,
    author?: string,
    year?: number | undefined,
    rate?: number | undefined,
    number?: string,
    authorSecond?: string,
    image?: string
}
